# Server
PORT=5000
NODE_ENV=development

# MongoDB Atlas connection string
# Get this from Atlas -> Database -> Connect -> Drivers
# Remember to URL-encode special characters in your password
MONGODB_URI=mongodb+srv://nilesh:nilesh5633@jobsportal.3vajkte.mongodb.net/

# JWT
JWT_SECRET=stinumjobswebsites
JWT_EXPIRES_IN=7d

# Comma-separated list of allowed frontend origins (no trailing slash).
# Use * only for local testing — never in production.
CORS_ORIGIN=http://127.0.0.1:5500,http://localhost:5500,https://rojgar-setu-puce.vercel.app

# Used ONLY by the seed script to create the first admin account.
# Change the password before you ever run the seed script for real.
ADMIN_USERNAME=admin
ADMIN_PASSWORD=Nilesh@5633
ADMIN_NAME=Nilesh
